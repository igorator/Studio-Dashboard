import { WrapperCard } from '../../components/Cards/WrapperCard/WrapperCard';

export function Team() {
  return (
    <WrapperCard title='Team' entityType='team'>
      Team
    </WrapperCard>
  );
}
